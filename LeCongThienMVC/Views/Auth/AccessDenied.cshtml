@{
    ViewData["Title"] = "Access Denied";
}

<div class="d-flex flex-column justify-content-center align-items-center vh-100 bg-light text-center px-3">
    <div class="mb-4">
        <i class="fas fa-exclamation-triangle fa-5x text-warning"></i>
    </div>
    <h2 class="mb-3 text-danger fw-bold">@ViewData["Title"]</h2>
    <p class="mb-4 fs-5">Có lỗi gì đó đã xảy ra Lmao.</p>
    <a asp-controller="Home" asp-action="Index" class="btn btn-primary px-4">Go Back Home</a>
</div>
